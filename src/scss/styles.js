const navbarHeight = 76

const heading = {
  fontSize: "4rem",
  fontWeight: "bold",
  lineHeight: 1,
}

const uppercased = {
  textTransform: "uppercase",
}

export { navbarHeight, heading, uppercased }
